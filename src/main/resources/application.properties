spring.application.name=boasse-backend

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://gateway01.ap-northeast-1.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY&useSSL=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:37kpdLUo7wPQZfp.root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:STENWTe0xYKCm54k}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# File Upload Configuration
file.dir=${FILE_DIR:uploads}
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Tomcat Configuration
server.tomcat.max-http-form-post-size=100MB
server.tomcat.max-parameter-count=10000

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,https://boasse-front.onrender.com,http://localhost:5173}


# Auth Configuration
auth.admin.password=${AUTH_ADMIN_PASSWORD:secure_password_123!}
jwt.secret=${JWT_SECRET:boassebackendsecretkeyforeachprojectshouldbeuniqueandlongenough20260123}
jwt.expiration=${JWT_EXPIRATION:86400000}